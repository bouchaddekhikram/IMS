# spring.datasource.url=jdbc:mysql://localhost:3306/Internship-Management-System?useSSL=false&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=
# #spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQL8Dialect
# spring.jpa.hibernate.ddl-auto= update

# # App Properties
# IMS.app.jwtSecret= IMSSecretKey
# IMS.app.jwtExpirationMs= 3600000
# IMS.app.jwtRefreshExpirationMs= 86400000
# server.port=8085

# ## For test
# #ahlem.app.jwtExpirationMs= 60000
# #ahlem.app.jwtRefreshExpirationMs= 120000

# # Mail Properties
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=raowajdi@gmail.com
# spring.mail.password=rsxvqzlrkyhuqxwv
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/Internship-Management-System?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# App Properties
IMS.app.jwtSecret=${IMS_APP_JWTSECRET:IMSSecretKey}
IMS.app.jwtExpirationMs=${IMS_APP_JWTEXPIRATIONMS:3600000}
IMS.app.jwtRefreshExpirationMs=${IMS_APP_JWTREFRESHEXPIRATIONMS:86400000}
server.port=${SERVER_PORT:8085}

# Mail Properties
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:raowajdi@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:rsxvqzlrkyhuqxwv}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
